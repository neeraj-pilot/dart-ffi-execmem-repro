name: cronet_execmem_repro
description: Isolated Cronet scenario for the Dart FFI executable-memory reproduction.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.9

dependencies:
  flutter:
    sdk: flutter
  cronet_http: 1.9.0
  dart_ffi_execmem_repro:
    path: ..

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
